# Prevent lifecycle scripts from running on install (supply chain protection)
ignore-scripts=true

# pnpm: reject packages published less than 7 days ago
# (prevents "fresh malicious package" attack vector)
# Ref: https://pnpm.io/npmrc#minimumreleaseage
minimum-release-age=10080

# Lock engine version
engine-strict=true
