// pnpm audit hook — if project migrates from npm to pnpm
// minimumReleaseAge prevents installing packages published < 10080 minutes (7 days) ago
// Run: pnpm install --audit --ignore-scripts

module.exports = {};
